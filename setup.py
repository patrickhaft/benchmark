from setuptools import setup, find_packages

setup(
    name = "benchmark_lib",
    version = "0.1",
    author = "Patrick Haft et al.",
    author_email = "patrick.haft@tum.de",
    packages=find_packages(include=['benchmark_lib']),
)