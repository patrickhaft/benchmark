from setuptools import setup, find_packages

setup(
    name = "BENCHMARK",
    version = "0.1",
    author = "Patrick Haft",
    author_email = "patrick.haft@tum.de",
    packages = find_packages(),
)